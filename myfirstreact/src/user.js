import { createSlice } from "@reduxjs/toolkit";

const intialStateValue = { fname: '', lname: '', designation: '' }

const userSlcie = createSlice(
    {
        name: 'user',
        intialState: { value: intialStateValue },
        reducer:

    }
)